{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "services/api/Dockerfile",
    "buildCommand": ""
  },
  "deploy": {
    "preDeployCommand": "npx prisma migrate deploy --schema=/app/packages/database/prisma/schema.prisma",
    "startCommand": "node /app/services/api/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "healthcheckPath": "/health"
  }
}
