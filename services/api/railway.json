{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "services/api/Dockerfile",
    "buildCommand": ""
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=/app/packages/database/prisma/schema.prisma && node /app/services/api/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "healthcheckPath": "/health"
  }
}
